const size = {
  xs: "320px",
  sm: "767px",
  md: "1023px",
};

const device = {
  smallMobile: `(max-width: ${size.xs})`,
  mobile: `(max-width: ${size.sm})`,
  tablet: `(max-width: ${size.md})`,
};

export { size, device };
