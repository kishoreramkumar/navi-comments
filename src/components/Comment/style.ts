import styled from "styled-components";

const CommentWrapper = styled.div`
  .comment-container {
    margin: 0.5rem 0;
    .comment-body {
      padding: 0.25rem 0;
    }
  }
`;

export { CommentWrapper };
